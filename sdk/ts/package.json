{
  "name": "@rhinestone/module-sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build:wasm": "cd ../webauthn-v2 && wasm-pack build --target bundler --out-dir ../ts/src/wasm/webauthn-v2",
    "build:ts": "tsc",
    "build": "npm run build:wasm && npm run build:ts",
    "test": "vitest"
  },
  "dependencies": {
    "viem": "^2.38.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
